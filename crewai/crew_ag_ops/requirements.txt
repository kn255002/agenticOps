# This file contains the dependencies required for the crew_ag_ops module.
# It is used to install the necessary packages for the module to function correctly.
# To install the dependencies, run:
# You should be in your virtual environment (python3 -m venv venv-project) before running the command below.
# pip install -r requirements.txt

crewai
openai
langchain
pydantic
fastapi
uvicorn
jinja2
paramiko
ansible-runner
pymupdf
python-dotenv
sqlalchemy